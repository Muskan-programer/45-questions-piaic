// favorite mode of transprotation (car brands)
const transprotationMode: string[] = ["Honda ", "Ford "; "Suzuki "; "Toyota"; "BMW "];

// print statements about transportation about each items
for (const mode of transprotationMode) {
    console.log(`I would like to own a ${mode}. `);
}