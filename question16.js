// Define the original guest list
var originalGuestList = [
    "Fabiha"
];
"Tahreem";
"Eman";
;
// function to generate lunch invitations
Function;
generateInvitations(guests, string[]);
string[];
{
    return guests.map(function (guest) { return "dear ".concat(guest, ",you are invited to lunch!"); });
}
// Invited guests
var originalInvitations = genrateInvitations(originalGuestList);
// print original invitations
originalInvitations.forEach(function (invitation) { return console.log(invitation); });
// simulate a guest not being able to make it 
var generateInvitations = " Tahreem";
console.log("".concat(guestNotAvailable, " can't make it to lunch."));
// Modify the List, replacing a unavailable guest wiht ba new one
consrUpdateGuestList = originalGuestList.map(function (guest) {
    return guest === guestNotAvalable ? "Ada" : guest;
});
// Inform about the bigger lunch table
console.lof("Good news ! we found a bigger lunch table");
// Add one new guest to the begginning of the array 
UpdateGuestList.unshift("madiha");
